# Parallel Export Test Configuration - 5 Workers (Default)
# Tests standard parallel export with default worker count

trustServerCertificate: true

export:
  parallel:
    enabled: true
    maxWorkers: 5
    progressInterval: 50
